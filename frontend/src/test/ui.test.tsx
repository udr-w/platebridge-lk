import {render,screen} from '@testing-library/react';import {describe,it,expect} from 'vitest';import {I18nextProvider} from 'react-i18next';import i18n from '../i18n';import {Badge,Safety} from '../components/UI';
describe('safety UI',()=>{it('renders classification with non-colour label',()=>{render(<I18nextProvider i18n={i18n}><Safety classification="RED" explanation="Not eligible"/></I18nextProvider>);expect(screen.getByText('Not eligible',{exact:true})).toBeInTheDocument();expect(screen.getByText('Not eligible for redistribution')).toBeInTheDocument()});it('localises statuses',async()=>{await i18n.changeLanguage('si');render(<I18nextProvider i18n={i18n}><Badge value="AVAILABLE"/></I18nextProvider>);expect(screen.getByText('ලබාගත හැක')).toBeInTheDocument();await i18n.changeLanguage('en')})});
